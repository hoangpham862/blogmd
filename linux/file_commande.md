

// Câu lệnh tạo thư mục mục:  mkdir  [tên thư mục hoặc đường dẫn để tạo các thư mục lồng nhau ]
// ls --> xem tất cả file cũng như thư mục mà vị trí hiện tại mình đang ở 
// pwd --> xem đường dẫn hiện tại mình đang ở vị trí nào trên terminal 


// nano --> truy cập vào một file để có thể edit chỉnh sửa nó: Sau khi làm xong cần Ctrl + O , enter để lưu nó lại rồi thoát ra
// grep --> Tìm kiếm chuỗi văn bản 

// ls -l ---> Hiện thông tin chi tiết của file/thư mục
// ls -d ---> Hiển thị chính thư mục đó và không hiển thị thư mục file bên trong nó 

//



